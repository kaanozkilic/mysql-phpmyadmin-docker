{
  "name": "MySQL Dev Container",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "db",
   "workspaceFolder": "/workspace",
  "mounts": [
    "source=${workspaceFolder},target=/workspace,type=bind,consistency=cached"]
}